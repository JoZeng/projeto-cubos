# PROJETO-CUBOS/.env
# Esta variável será usada SOMENTE se import.meta.env.DEV for FALSE e você não tiver um valor padrão no código.
# No seu caso, a lógica `|| (import.meta.env.DEV ? ...)` já cobre isso.
# Você pode até omitir esta linha no .env se quiser, mas tê-la pode ser útil para clareza.
# VITE_API_BASE_URL=http://localhost:3000/api